---
import {
  VueIcon,
  NextIcon,
  NuxtIcon,
  ReactIcon,
  MysqlIcon,
  DockerIcon,
  FlutterIcon,
  LaravelIcon,
  TailwindIcon,
  BootstrapIcon,
} from '@/common/Icons'
import HomeKnowledgeProgress from './HomeKnowledgeProgress.astro'

const iconSvgClass = 'w-12 h-12 md:w-8 md:h-8'
const iconNameClass = 'text-xs tracking-tight uppercase'
---

<section class="space-y-4">
  <h3 class="text-center text-xl font-bold text-blue-400 uppercase">
    Knowledge
  </h3>
  <p class="text-center">Some of the technologies I use frequently.</p>
  <div class="grid grid-cols-2 sm:grid-cols-3 gap-0.5">
    <HomeKnowledgeProgress percent={90}>
      <ReactIcon className={iconSvgClass} />
      <p class={iconNameClass}>react</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={90}>
      <NextIcon className={iconSvgClass} />
      <p class={iconNameClass}>next</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={90}>
      <LaravelIcon className={iconSvgClass} />
      <p class={iconNameClass}>laravel</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={50}>
      <VueIcon className={iconSvgClass} />
      <p class={iconNameClass}>vue</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={50}>
      <NuxtIcon className={iconSvgClass} />
      <p class={iconNameClass}>nuxt</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={50}>
      <FlutterIcon className={iconSvgClass} />
      <p class={iconNameClass}>flutter</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={90}>
      <TailwindIcon className={iconSvgClass} />
      <p class={iconNameClass}>tailwind</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={90}>
      <BootstrapIcon className={iconSvgClass} />
      <p class={iconNameClass}>bootstrap</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={60}>
      <MysqlIcon className={iconSvgClass} />
      <p class={iconNameClass}>mysql</p>
    </HomeKnowledgeProgress>
    <HomeKnowledgeProgress percent={30}>
      <DockerIcon className={iconSvgClass} />
      <p class={iconNameClass}>docker</p>
    </HomeKnowledgeProgress>
  </div>
  <p class="text-center">Also, I use GIT for project version control.</p>
</section>
